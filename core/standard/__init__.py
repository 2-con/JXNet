"""
# Standard
A module that contains the necessary components for StandardNet to work. Most components are modular, which means custom components can be added
provided they adhere to the provided base class.

### Callbacks
  A sub-module containing callbacks that StandardNet accepts as valid instructions for training

### Data Handlers
  A sub-module containing data handlers that StandardNet use to process data before training

### Encoders
  A sub-module containing encoders that serve as a utility for workflows

### Functions
  A sub-module containing functions and scalers that StandardNet layers accepts as valid operations

### Initializers
  A sub-module containing initializers that StandardNet layers accepts as valid parameter initializers

### Layers
  A sub-module containing layers that StandardNet accepts as valid layers

### Losses
  A sub-module containing losses that StandardNet accepts as valid loss functions

### Metrics
  A sub-module containing metrics that StandardNet accepts as valid metrics only for classification models

### Optimizers
  A sub-module containing optimizers that StandardNet accepts as valid optimizers
"""